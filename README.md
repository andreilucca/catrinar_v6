# catrinar_v6

Mihai Catrinar V6 — cinematic personal website with a new layout and interactive 3D animations.

## Local preview

Because the site is a static HTML/CSS/JS build, you can preview it locally with:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
